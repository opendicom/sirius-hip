FROM  rockylinux:8.7

RUN dnf install -y \
    openssl-devel \
    gcc && \
    curl https://sh.rustup.rs -sSf | sh -s -- -y

WORKDIR /usr/src/sirius-hip

COPY . .
RUN source $HOME/.cargo/env && \ 
    cargo install --path .
